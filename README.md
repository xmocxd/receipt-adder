# receipt-adder


show a list of the 10 last added numbers to the column where this adds numbers in spreadsheet -- just grab the data from sheets starting at the cell / data_start_row which it is currently on for the week, and truncate the last 10 items, displaying in reverse order.

then add a visual gradient effect to fade out these numbers toward the bottom of the list

update this list and show the last added number in a different color when the form is submitted

also show the "type" cell if anything present (cell directly to the right of each number) in this same list


---

add an undo button which would remove the last added data to the spreadsheet -- just allow for a single level of undo/redo
